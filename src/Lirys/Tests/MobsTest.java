package Lirys.Tests;

import Lirys.characters.mobs.Mobs;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MobsTest {
    private static Mobs mob;

    @Test
    void generateMobs() {
        Assertions.assertTrue(true); //passed true, since method only instantiates objects
    }
}